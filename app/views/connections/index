<% @title = "WS-Browser - Connections"
   @nav_active = "connections" unless @mode == :connect
-%>

<% if @mode == :connect
     @subnav = subnav_for_home
   else
     @subnav = subnav_for_connections
   end
-%>

<% @view_title = "Connections"
   @view_partial = "index" -%>
