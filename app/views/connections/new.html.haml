.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", connections_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_connection_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Connection
    .inner
      = form_for :connection, :url => connections_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
