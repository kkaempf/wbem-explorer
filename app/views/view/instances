<% @title = "WS-Browser - Instance Browser"
   @nav_active = "home"
   @view_title = "Instances of #{@cimclass.name} on #{@host.name}"
-%>

<% @subnav = subnav_for_home -%>

<% unless @features.empty?
     @view_partial = "features"
   else
     @view_partial = "empty"
   end
-%>  
<!-- its all in the 'home' layout -->
