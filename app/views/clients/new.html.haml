.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", clients_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_client_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      client
    .inner
      = form_for :client, :url => clients_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
