:plain
[
- first = true
- @namespaces.each do |ns|
  - if first
    - first = false
  - else
    ,
  :plain
      {
        "controller": "classnames",
        "ns" : "#{ns.name}",
        "icon": false,
        "title": "#{ns.name}"
      }
]

