:plain
[
- first = true
- @namespaces.sort.each do |ns|
  - if first
    - first = false
  - else
    ,
  :plain
      {
        "key": "namespaces?ns=#{ns}",
        "icon": false,
        "title": "#{ns}"
      }
]

